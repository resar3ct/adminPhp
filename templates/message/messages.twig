{% extends "base.twig" %}

{% block body %}
    <ul class="list-group">
        {% for message in messages %}
            <li class="list-group-item"> <b> {{ message.title }} </b>
                <p> {{message.body }} </p>
            </li>
        {% endfor %}
    </ul>
{% endblock %}